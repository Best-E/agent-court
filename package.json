{
  "name": "agent-court",
  "version": "1.0.0",
  "description": "ERC-7500: Agent Court - Identity, Payments, Disputes for AI Agents",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "verify": "hardhat run scripts/verify.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.0"
  },
  "keywords": ["erc-7500", "ai-agents", "ethereum", "payments", "disputes"],
  "license": "Apache-2.0"
}
